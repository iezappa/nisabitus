tags:
  # Screen captures under test/screenshots. They are goldens, so they carry
  # the SDK's own font rendering: `flutter test -x screenshots` skips them
  # when only the behaviour matters.
  screenshots:
